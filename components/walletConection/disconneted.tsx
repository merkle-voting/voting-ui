import Button from '../styled/Button';

const Disconnected = () => {
    return (
        <Button borderRadius="2rem" padding=".5rem 1.5rem" fontWeigth="500">
            Connect wallet
        </Button>
    );
};

export default Disconnected;
